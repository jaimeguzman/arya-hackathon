sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
neo4j>=5.26.0
redis>=5.2.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
twilio>=9.3.0
httpx>=0.27.0
pymupdf>=1.24.0
Pillow>=10.4.0
# Two Gemini SDKs, deliberately: gemini_client.py (services/) uses the legacy
# google-generativeai chat/session API; backend/voice/guardrails.py uses the
# current google-genai SDK. Unifying them is a separate task, not done here
# to avoid risking the Document Pipeline's vision-extraction call sites.
google-generativeai>=0.8.0
google-genai>=1.0.0
# Local Phase 5/6 stack (Orchestrator, Follow-up) is verified on langgraph 0.3.x —
# empirically confirmed 2026-07-11 (21/25 orchestrator+followup tests pass on 0.3.34;
# the 4 failures are a pre-existing test-isolation issue, not a version incompatibility).
# Do not bump to 1.x without re-verifying the orchestrator test suite.
langgraph>=0.2,<0.4
pytest>=8.0.0
pytest-asyncio>=0.24.0
